{
  "name": "media-buying",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd web && npm install && npm run build && cd ../api && npm install && npm run build",
    "start": "cd api && npm run start",
    "dev:api": "cd api && npm run dev",
    "dev:web": "cd web && npm run dev"
  }
}
