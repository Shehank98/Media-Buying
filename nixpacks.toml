# Ensure the PostgreSQL client tools (pg_dump) are available in the runtime
# image so the daily Google Drive backup can dump the database. aptPkgs is
# appended to the auto-detected setup phase, so Node detection is unaffected.
[phases.setup]
aptPkgs = ["postgresql-client"]
